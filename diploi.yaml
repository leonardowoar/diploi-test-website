kind: StaticSite         # Bu projenin statik HTML olduğunu söyler
name: diploi-test-site   # Proje adı
source:
  root: /                # index.html ana dizinde
deploy:
  builder: static        # build yok çünkü HTML projesi
  output: /              # yayınlanacak dizin
 